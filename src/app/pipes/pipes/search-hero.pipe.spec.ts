import { SearchHeroPipe } from '../search-hero.pipe';

describe('SearchHeroPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHeroPipe();
    expect(pipe).toBeTruthy();
  });
});
